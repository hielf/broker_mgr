<div class="row">
	<div class="col-md-9">
		<div class="well">
			<legend><p class="lead text-center muted">任务指派</p></legend>
				<%= simple_form_for @custservvisit, :defaults => { :wrapper_html => {:class => 'form-group'},  :html => { :class => 'form-horizontal'} } do |f| %>
			
					<%= render 'fields', :f => f %>
					<%= f.submit "确认", :class => 'btn btn-primary btn-sm' %>
					<%= link_to '取消', root_path, :class => 'btn btn-default btn-sm' %>
				<% end %>
		</div>
		
		</div>
		<div class="col-md-3">
			<div class="alert alert-warning alert-dismissable">
				<button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
			  <h4>帮助</h4>
			  工作流程在此处定义。
			</div>
		</div>
</div>